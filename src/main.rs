use gdlauncher_entry::*;

fn main() {
    // println!("{:#?}", no_dups::solve());
    // println!("{:#?}", no_idx::solve());
    // println!("{:#?}", cyclic::solve());
    // println!("{:#?}", w_counter::solve());
    println!("{:#?}", counter_n_cyclic::solve());
}
